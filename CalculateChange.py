#returns area of rectangle - Python

def main():
    width = float(input('Enter width:'))  	# input prompt
    length = float(input('Enter length:')) # input prompt
    area = width * length
    print('Area is {0:7.2f}'.format(area)) # output
main()
